name: pegasus_backend
description: A local server app using the shelf package and routing for the Pegasus MVP.
version: 1.0.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  lints: ^2.0.0
